# Dependencies
node_modules/

# Environment / secrets — never commit a real Marky API key
.env
.env.local
.env.*.local

# Per-user, cross-session plugin state. The real file holds local prefs (feedback
# cadence, contribution nudge timing) and must never be committed. The committed
# `user.toml.example` documents the schema; each user copies it to user.toml.
user.toml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Agent-maintained brand voice cache (per-user, injected by the SessionStart hook)
brand-voice.md
